config:
    target: 'http://localhost:8000'
    phases:
        - duration: 5
          arrivalRate: 1000
    defaults:
        headers:
            Content-Type: 'application/json'

scenarios:
    - name: Get sale status
      flow:
          - get:
                url: '/sales'
